"""
Otter Grader utilities for the Confidence Interval notebook.
Tests are defined in the notebook (between # BEGIN TESTS and # END TESTS).
Use grader.check("q1"), grader.check("q2"), grader.check("q3"),
or grader.check_all() in the notebook.
After running otter assign, the generated student notebook has tests in its metadata.
"""

import otter

grader = otter.Notebook()
